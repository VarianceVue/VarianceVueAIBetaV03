# Use ONE of these (Claude is used if ANTHROPIC_API_KEY is set; otherwise OpenAI).

# Option A: Claude (Anthropic) — no OpenAI billing required
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Option B: OpenAI
# Get a key at https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-key-here

# Optional: Claude model (default: claude-3-5-sonnet-latest)
# ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# Optional: OpenAI model (default: gpt-4o-mini)
# OPENAI_CHAT_MODEL=gpt-4o

# Optional: path to skill file if not using default
# SCHEDULE_AGENT_SKILL_PATH=/path/to/SKILL.md
